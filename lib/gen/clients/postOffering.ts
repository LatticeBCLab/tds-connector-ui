/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@kubb/plugin-client/clients/axios'
import type { RequestConfig, ResponseErrorConfig } from '@kubb/plugin-client/clients/axios'
import type { PostOfferingMutationRequest, PostOfferingMutationResponse, PostOffering400, PostOffering500 } from '../types/PostOffering.ts'

function getPostOfferingUrl() {
  const res = { method: 'POST', url: `/tdsc/offering` as const }
  return res
}

/**
 * @description 创建一个新的 Offering 并上传文件到 MinIO
 * @summary 创建 Offering（上传文件）
 * {@link /offering}
 */
export async function postOffering(
  data: PostOfferingMutationRequest,
  config: Partial<RequestConfig<PostOfferingMutationRequest>> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const requestData = data
  const formData = new FormData()
  if (requestData) {
    Object.keys(requestData).forEach((key) => {
      const value = requestData[key as keyof typeof requestData]
      if (typeof value === 'string' || (value as unknown) instanceof Blob) {
        formData.append(key, value as unknown as string | Blob)
      }
    })
  }
  const res = await request<PostOfferingMutationResponse, ResponseErrorConfig<PostOffering400 | PostOffering500>, PostOfferingMutationRequest>({
    method: 'POST',
    url: getPostOfferingUrl().url.toString(),
    data: formData,
    ...requestConfig,
    headers: { 'Content-Type': 'multipart/form-data', ...requestConfig.headers },
  })
  return res.data
}