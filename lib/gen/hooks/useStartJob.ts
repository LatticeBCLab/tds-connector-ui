/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@kubb/plugin-client/clients/axios'
import type { RequestConfig, ResponseErrorConfig } from '@kubb/plugin-client/clients/axios'
import type { UseMutationOptions, QueryClient } from '@tanstack/react-query'
import type { StartJobMutationResponse, StartJobPathParams, StartJob400, StartJob404, StartJob500 } from '../types/StartJob.ts'
import { useMutation } from '@tanstack/react-query'
import { startJob } from '../clients/startJob.ts'

export const startJobMutationKey = () => [{ url: '/api/v1/jobs/:id/start' }] as const

export type StartJobMutationKey = ReturnType<typeof startJobMutationKey>

/**
 * @description 启动指定的Job
 * @summary 启动Job
 * {@link /api/v1/jobs/:id/start}
 */
export function useStartJob<TContext>(
  options: {
    mutation?: UseMutationOptions<
      StartJobMutationResponse,
      ResponseErrorConfig<StartJob400 | StartJob404 | StartJob500>,
      { id: StartJobPathParams['id'] },
      TContext
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {},
) {
  const { mutation = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...mutationOptions } = mutation
  const mutationKey = mutationOptions.mutationKey ?? startJobMutationKey()

  return useMutation<StartJobMutationResponse, ResponseErrorConfig<StartJob400 | StartJob404 | StartJob500>, { id: StartJobPathParams['id'] }, TContext>(
    {
      mutationFn: async ({ id }) => {
        return startJob(id, config)
      },
      mutationKey,
      ...mutationOptions,
    },
    queryClient,
  )
}